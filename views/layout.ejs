<!DOCTYPE html>
<html lang="en">
  <%- include("partials/head") %>
  <body>
    <% if (title !== 'Login' && title !== 'Register') { %>
      <%- include("partials/header") %>
    <% } %>

    <main>
      <%- body %>
    </main>

    <% if (title !== 'Login' && title !== 'Register') { %>
      <%- include("partials/footer") %>
    <% } %>
  </body>
</html>
